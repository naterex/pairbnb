<div class="row">
  <div class="col-sm-7 col-sm-offset-2 col-md-6 col-md-offset-3 col-lg-4 col-lg-offset-4" id="listing">
  <center>
    <h2><%= t(".my_listings") %></h2>
  </center>

  <% @listings.each do |listing| %>
    <div class="row">
      <h3>
        <%= link_to listing.title, listing_path(listing) %>
      </h3>
    </div>

  <% end %>


  </div>
</div>
